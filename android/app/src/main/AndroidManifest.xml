<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ── Internet ──────────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- ── BLE — Android ≤11 (API ≤30) — Z35 ফোন Android 11 ────────── -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <!-- Android ≤11: BLE scan করতে Location permission বাধ্যতামূলক -->
    <!-- [FIX-BLE-GATT] maxSdkVersion="30" সরানো হয়েছে: কিছু Android 12+ OEM ROM -->
    <!-- (Samsung OneUI, Xiaomi MIUI) এ GATT connect phase এ location লাগে।         -->
    <!-- BLUETOOTH_SCAN এ neverForLocation flag আছে তাই scan এ location দরকার নেই। -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- ── BLE — Android 12+ (API 31+) ──────────────────────────────── -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"
        tools:targetApi="s"/>

    <!-- ── Notifications ─────────────────────────────────────────────── -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- ── Phone state (for auto country code detection via TelephonyManager) ── -->
    <!-- Needed by MainActivity.kt: getSimCountryIso(), getNetworkCountryIso()     -->
    <!-- READ_PHONE_STATE is a normal permission — no user dialog on Android < 10  -->
    <!-- On Android 10+: auto-granted at install, no runtime request needed        -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <!-- BLE hardware — required:false so non-BLE devices can install -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="false"/>

    <application
        android:label="SMART WATER LEVEL CONTROL BD"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:allowBackup="false"
        android:hardwareAccelerated="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data android:name="flutterEmbedding" android:value="2"/>

        <!-- ── SMS Retriever API — Android auto-reads OTP without RECEIVE_SMS ─── -->
        <!-- Firebase Phone Auth uses SMS Retriever for seamless auto-verification  -->
        <!-- [FIX-OTP] Enables OTP auto-fill on Android 8+ without RECEIVE_SMS     -->
        <receiver android:name="com.google.android.gms.auth.api.phone.RetrieveDiversionReceiver"
            android:exported="true"
            android:permission="com.google.android.gms.auth.api.phone.permission.SEND"
            tools:targetApi="tiramisu">
            <intent-filter>
                <action android:name="com.google.android.gms.auth.api.phone.SMS_RETRIEVED"/>
            </intent-filter>
        </receiver>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Google Sign-In account picker -->
        <intent>
            <action android:name="com.google.android.gms.auth.GOOGLE_SIGN_IN"/>
        </intent>
    </queries>
</manifest>
